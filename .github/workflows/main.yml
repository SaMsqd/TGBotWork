name: Publish to server

on:
  push:
    branches: [ "master" ]

jobs:
  server_update:
    name: Update server buy ssh
    runs-on: ubuntu-latest
    steps:
      - name: Connect and run script
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: 185.41.160.212
          port: 22
          username: root
          password: SaintSquad.12
          script_stop: true
          script: |
            echo "Download from git"
            cd TGBotWork
            git fetch
            echo "DOCKER build"
            docker build -t bot .
            echo "DOCKER run"
            docker stop bot
            docker rm bot
            docker image rm bot
            docker run --name bot -t -d bot
            echo "Finish deploying!"